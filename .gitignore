# Virtual environments
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Raw feed cache and full generated output (regenerated by running the scraper).
# sample_output.json IS committed so reviewers can see the shape without running.
raw_feed.xml
willabgarden_products.json

# Enrichment page cache (regenerated on demand) and scratch/verification files.
.page_cache/
_verify.json
_*.json

# Internal build brief and personal interview-prep report (not for publication)
willabgarden-scraper-brief.md
ENGINEERING_REPORT.md

# OS / editor / tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
